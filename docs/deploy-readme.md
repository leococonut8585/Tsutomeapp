# Tsutome デプロイ手順メモの参照先

本番サーバーへの接続情報・SSH 鍵・詳細なデプロイ手順は **Git 管理対象外** のローカルメモにまとめています。

- 保存場所: `C:\Users\dokog\AppDev\Tsutomeapp\local-notes\tsutomeapp-deploy-notes.md`
- 同ディレクトリに `tsutomeapp_ed25519` (秘密鍵) / `tsutomeapp_ed25519.pub` があり、Git には含めません。
- ファイルが存在しない場合は上記パスに再作成し、公開鍵をサーバーの `~/.ssh/authorized_keys` に登録してから利用してください。

